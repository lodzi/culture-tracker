{
  "name": "culture-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight daily culture brief — static frontend + Node.js script that aggregates RSS feeds. No API keys required.",
  "scripts": {
    "update": "node scripts/fetch-and-summarize.js",
    "synthesize": "node scripts/ai-synthesize.js",
    "email": "node scripts/send-email.js",
    "weekly-email": "node scripts/send-weekly-email.js",
    "daily": "npm run update && npm run synthesize && npm run email",
    "check-sources": "node scripts/check-sources.js",
    "serve": "npx --yes http-server . -p 8080 -c-1"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "nodemailer": "^6.9.14",
    "rss-parser": "^3.13.0"
  }
}
